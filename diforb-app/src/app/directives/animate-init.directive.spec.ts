import { AnimateInitDirective } from './animate-init.directive';

describe('AnimateInitDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateInitDirective();
    expect(directive).toBeTruthy();
  });
});
