import { SplitTextDirective } from './split-text.directive';

describe('SplitTextDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitTextDirective();
    expect(directive).toBeTruthy();
  });
});
